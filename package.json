{
  "name": "field-notes",
  "version": "1.0.0",
  "description": "A local, single-user nature log. No dependencies, no build step.",
  "private": true,
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "node --test test/*.test.js",
    "lint": "node scripts/lint.js"
  },
  "engines": {
    "node": ">=18"
  },
  "license": "UNLICENSED"
}
